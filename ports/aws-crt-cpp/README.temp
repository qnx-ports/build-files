build instructions for internal eyes:
- git clone build files and aws-crt-cpp from qnx-ports
- cd to aws-crt-cpp repo and do submodule update (recursive)
- cd - and build using `make -C build-files/ports/aws-crt-cpp -j24`. for arch specific use `OSLIST=<arch> make ...`
